export interface childPayload {
    id: string;
    name: string;
    age: string;
    dob?: Date;
}

export interface ChildListItem {
  id: string;
  name: string;
  age: number;
  avatar: string;
  status: string;
}